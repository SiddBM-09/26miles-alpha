Place the 26 Miles Capital logo image here as `26-miles-logo.png`.

Recommended:
- File path: `/public/assets/26-miles-logo.png`
- Format: PNG (or WebP)
- Height: ~28-40px (the layout uses `h-7`, ~28px)
- Width: auto; keep aspect ratio

If you prefer a different filename, update `src/components/AppShell.tsx` image `src` accordingly.
